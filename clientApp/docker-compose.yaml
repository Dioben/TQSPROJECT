version: "3.8"
services:
  backend:
    image: dioben/tqs-client-server
    ports:
      - 8000:8000
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mysql:8.0
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "--silent"]
      interval: 3s
      retries: 5
      start_period: 30s
    volumes:
      - /client-db-data:/var/lib/mysql  
    ports:
      - 3300:3306
    environment:
      - MYSQL_ROOT_PASSWORD= mysqlpassword
      - MYSQL_DATABASE= delivery_service
